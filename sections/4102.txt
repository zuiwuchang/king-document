var __v="\u003cpre class=\"k-pre\"\u003e旋轉 位移 縮放 上遊戲最常用的功能 基本每個節點都會用到\n在物理系統中 系統會 自動同步這些屬性與box2d中的屬性\n\nbox2d只有旋轉 位移 沒有縮放 \u003cstrong\u003e通過節點設置 縮放 會重新創建剛體依賴的 碰撞體 \n避免的方式 是將 渲染節點設置爲剛體的子節點\n縮放時 只縮放渲染節點\u003c/strong\u003e\n\n每個物理時間之後會把所有剛體信息同步到對於節點上\n出於性能考慮 節點信息只有在用戶節點相關射線進行顯示設置時才同步到剛體上 並且剛體只會監視它所在節點\u003cstrong\u003e(既如果修改了父節點的旋轉位移不會同步這些信息)\u003c/strong\u003e\n\u003c/pre\u003e"