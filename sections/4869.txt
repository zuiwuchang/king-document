var __v="\u003cpre class='k-pre'\u003epipe 管道 可以將 多個 操作符 串連起來\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003eimport { Component, OnInit } from '@angular/core';\nimport { of } from \"rxjs/observable/of\";\nimport { pipe } from \"rxjs\";\nimport { map, filter } from 'rxjs/operators';\n\n@Component({\n  selector: 'app-view1',\n  templateUrl: './view1.component.html',\n  styleUrls: ['./view1.component.css']\n})\nexport class View1Component implements OnInit {\n\n  constructor() { }\n\n  ngOnInit() {\n    // 創建 Observable\n    let observable = of(0, 1, 2, 3);\n    // map 操作符\n    const squareValues = pipe(\n      filter((v: number) =\u0026gt; {// 過濾陰數\n        return v % 2 == 1; //返回 false 的會被 過濾\n      }),\n      map((v: number) =\u0026gt; { //返回 平方\n        return v * v;\n      })\n    )\n    // 使用 操作符 包裝原 Observable 為 新的 Observable\n    observable = squareValues(observable);\n\n    // 訂閱\n    observable.subscribe({\n      next(v) { console.log(v); },\n      error(e) { console.error('Error: ' + e); },\n      complete() { console.log('Completed'); }\n    });\n  }\n}\n\u003c/pre\u003e\n\n\u003cpre class='k-pre'\u003e除了 輔助函數 pipe 外 Observable 也提供了同樣功能的 成員函數 pipe\n寫起來 讓代碼 更簡單\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003eimport { Component, OnInit } from '@angular/core';\nimport { of } from \"rxjs/observable/of\";\nimport { map, filter } from 'rxjs/operators';\n\n@Component({\n  selector: 'app-view1',\n  templateUrl: './view1.component.html',\n  styleUrls: ['./view1.component.css']\n})\nexport class View1Component implements OnInit {\n\n  constructor() { }\n\n  ngOnInit() {\n    // 創建 Observable\n    const observable = of(0, 1, 2, 3).pipe(\n      filter((v: number) =\u0026gt; {// 過濾陰數\n        return v % 2 == 1; //返回 false 的會被 過濾\n      }),\n      map((v: number) =\u0026gt; { //返回 平方\n        return v * v;\n      })\n    );\n\n    // 訂閱\n    observable.subscribe({\n      next(v) { console.log(v); },\n      error(e) { console.error('Error: ' + e); },\n      complete() { console.log('Completed'); }\n    });\n  }\n}\n\u003c/pre\u003e"