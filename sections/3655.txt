var __v="\u003cpre class=\"k-pre\"\u003elogin shell\t\t\t取得bash時 需要完成對 登入 如(tty1~tty6)\n\nnon-login shell\t不需要輸入密碼 時獲取的shell\n\t\t\t\t\t(如在 X window 中 打開 終端機 / 在bash中 進入另一個bash)\n\n\n\n//系統整體 設置 (只有 login shell 會執行)\n/etc/profile\n\n//按照如下順序 執行用戶 設置 (只有 login shell 會執行)\n~/.bash_profile\n~/.bash_login\n~/.profile\n\n//執行用戶 設置 \n\u003cstrong\u003e//(只有 non-login shell 會執行)\n//(但一般 ~/.bash_profile 等會 加載 ~/.bashrc  故對 login shell 通常亦有效 )\u003c/strong\u003e\n~/.bashrc \n\n//將 src 導入到 當前bash環境\nsource src\n. src\n\tsource ~/.bashrc\n\t. ~/.bashrc\n\u003c/pre\u003e"