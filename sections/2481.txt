var __v="\u003cpre class=\"k-pre\"\u003e將字符串 和數字計算 其將自動被 轉換為數字 \n\n.. 用於連接字符串 對於兩個數字 .. 需要以 空格 隔開 否則會被認為是 小數點\nprint(1 .. 2)\n\n\n10 == \"10\" 為假\n(其型別不同)\n\n最好顯示的 使用字符串 數字的 轉換\ntostring(number)\t--將 數字 轉換為 字符串 \ntonumber(string)\t--將 字符串 轉換為 數字\n\n在字符串前 加上 # 可以返回其長度 print(#\"123\");\n\u003c/pre\u003e"