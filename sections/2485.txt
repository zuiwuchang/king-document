var __v="\u003cpre class=\"k-pre\"\u003e使用 {} 創建 table \n當沒有任何 變量 持有 table 時  table 將自動被 釋放\n\u003cstrong\u003e除 像數組一樣 使用[] 指定key值外 亦可 使用 . \u003c/strong\u003e\n\na\t=\t{}\na['name']\t=\t\"illusive man\"\na.hp\t\t=\t1\na.mp\t\t=\t{}\na.mp.max\t=\t10\n\nprint(a['name'])\nprint(a.name)\nprint(a.hp);\nprint(a.mp.max);\nprint(a.mp.min);\t--nil\n\nprint(table.maxn(a))\t--返回 table 最大數值 索引 此處為0 \nprint(#a)\t\t\t\t--返回 長度 0 因為 table 不是線性的\n\na\t=\tnil\n\u003c/pre\u003e"