var __v="\u003cpre class=\"k-pre\"\u003e上述迭代器 每次遍歷 都需要 重新創建一個 迭代器實例(存儲遍歷位置)\n無狀態迭代器 可以使所有 遍歷都是用 同一個實例\n\n\n\n無狀態迭代器 需要 返回三個值 遍歷函數iter,被遍歷的恆定量a,初始遍歷索引index\nfor 每次循環 自動調用 iter(a,index)\n\u003c/pre\u003e"