var __v="\u003cpre class='k-pre'\u003egRPC 可以很容易的 支持 TLS\n只需要 在創建 服務 和 客戶端 時 傳入 TransportCredentials 相關配置 即可\n\ngoogle.golang.org/grpc/credentials 包的 \u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e//創建 rpc 服務器\ncreds, e := credentials.NewServerTLSFromFile(\"server.pem\", \"server.key\")\nif e != nil {\n\tlog.Fatalln(e)\n}\ns := grpc.NewServer(\n\t//設置 tls\n\tgrpc.Creds(creds),\n)\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e//連接服務器\ncreds := credentials.NewTLS(\u0026amp;tls.Config{\n\tInsecureSkipVerify: true,\n})\nconn, e := grpc.Dial(\n\tAddr,\n\tgrpc.WithTransportCredentials(creds),\n)\u003c/pre\u003e"