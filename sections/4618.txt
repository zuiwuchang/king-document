var __v="\n\u003cpre class='prettyprint linenums'\u003evar gulp = require('gulp');\nvar uglify = require('gulp-uglify');  //壓縮 插件\nvar rename = require('gulp-rename');  //改名 插件\n\ngulp.task('default', function () {\n  //執行 壓縮\n  return gulp.src('src/**/*.js') //輸入 多個目錄可以使用 ['xxx','xxx']\n    .pipe(uglify()) //執行壓縮\n    .pipe(rename({ suffix: '.min' })) //為輸出檔案 添加 .min 後綴\n    .pipe(gulp.dest('dist')); //輸出 目錄\n});\u003c/pre\u003e"