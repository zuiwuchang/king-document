var __v="\u003cpre class='k-pre'\u003e以為 docker 虛擬了網卡 所以只要知道 容器 ip就可以訪問\n為了不依賴 ip docker run 支持 --link 參數 可以將 指定容器主機 link 到當前運行 容器 以便 當前容器可以使用 主機名 而非 ip訪問 其它容器\ndocker run \u003cstrong\u003e--link 被連接的容器:可選的別名\u003c/strong\u003e\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e# 運行 kt1 容器 並將 kt2 容器 連接進來 別名為 web\nsudo docker run -it --name kt1 --link kt2:web -v /home/king/test:/tshare ubuntu /bin/bash\u003c/pre\u003e"