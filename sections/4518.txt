var __v="\u003cpre class='k-pre'\u003e實際上 無論是 signal 還是 slot 標籤 go qt 生成的 qt代碼 都是 一個 qt signal\n故 go 也可以 coonnect 使用 signal 標籤 聲明的 signal\n(以便 也能 得到 此 signal 通知)\n\n因爲 slot 標籤 實際也是 qt signal 所以 go 可以多次 connect 這個 同名的 signal\n只是 go qt 會自動 將 最後一個 connect 函數的 返回值 返回給 qml\n(如果 go 沒有 connect 則會返回一個 默認值)\n\n因爲實際是 就是 qt的 signal 沒有任何特殊的 故亦可執行 disconnect 操作\n(同時 go qt 提供了一個 DisconnectXXX 可以直接將 某名稱的 signal 全部 disconnect)\n\u003c/pre\u003e"