var __v="\u003cpre class=\"k-pre\"\u003eio_service::run 如果沒有 等待都異步事件就會 返回\n對於 send/write 的 io_service 某一時刻 可能會 沒有事件 (沒有連接成功都 socket 被分配到 此io_service)\n爲了 防止 io_service 返回 爲其投遞了一個 一直等待都 deadline_timer::async_wait\n\n需要使用合理都 方式將 socket 分配到不同的 io_service\n例子中 只是簡單的 爲每個 io_service 創建了一個(std::size_t* _clients = NULL;) std::size_t 在其中記錄了 分配都 socket 數量\n並在 分配 socket 時 將其分配到 連接數量最少都 io_service\n\u003c/pre\u003e"