var __v="\u003cpre class='k-pre'\u003eWaitGroup 通常 用於 等待 goroutine 結束\n其只提供了 3個 API\n\n// delta \u0026gt; 0 增加要等待的 數量 delta \u0026lt; 0 減少要等待的 數量\nfunc (wg *WaitGroup) Add(delta int)\n\n// 相當於 Add(-1)\nfunc (wg *WaitGroup) Done()\n\n// 等待 直到要 等待的數量為0時 返回\nfunc (wg *WaitGroup) Wait()\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003epackage main\n\nimport (\n\t\"log\"\n\t\"sync\"\n)\n\nfunc main() {\n\tvar wg sync.WaitGroup\n\twg.Add(4)\n\twg.Add(-2)\n\twg.Add(-1)\n\twg.Done()\n\twg.Wait()\n\tlog.Println(\"end\")\n}\n\u003c/pre\u003e"