var __v="\u003cpre class=\"k-pre\"\u003e數組是一個定長序列 其中每個元素型別相同\n多維數組不過是其元素 爲另外一個數組罷\n\n使用如下語法創建數組\n[N]type\n[N]type{v1,v2...}\n[...]type{v1,v2,...}\t//自動計算長度\n\n數組亦可被切片 不過其返回的將是切片 而非 數組\n數組可像切片一樣操作 不過 不可以 改變大小而\n\u003c/pre\u003e"