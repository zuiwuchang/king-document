var __v="\u003cpre class=\"k-pre\"\u003eassign 支持全部的 STL 標準容器(vector string deque list set multiset map multimap)\n對於非標準容器 stack queue priority_queue 沒有insert push_* 只能用 list_of 且最後要調用 to_adapter()\n\n\n\u003cpre class='prettyprint linenums'\u003e#include \u0026lt;stack\u0026gt;\n\n#include \u0026lt;boost/assign.hpp\u0026gt;\n\nint _tmain(int argc, _TCHAR* argv[])\n{\n\tstd::stack\u0026lt;int\u0026gt; stack = (boost::assign::list_of(1),2,3).to_adapter();\n\n\tstd::system(\"pause\");\n\treturn 0;\n}\u003c/pre\u003e\n\u003c/pre\u003e"