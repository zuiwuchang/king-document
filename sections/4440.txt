var __v="\u003cpre class='k-pre'\u003e代碼 15行的 buffer 大小需要自己估算下 需要保證 25的 alcCaptureSamples 寫入 buffer 時 buffer 足夠\n\nalcGetIntegerv 得到的 實際 採樣率 可能會 和alcCaptureOpenDevice的設置有出入\n\nalcCaptureSamples copy到buffer 的大小 是 alcGetIntegerv得到的 採樣率 依據格式 而不同\n\u003cstrong\u003eAL_FORMAT_MONO8\t\tsample * 1\nAL_FORMAT_MONO16\tsample * 2\nAL_FORMAT_STEREO8\tsample * 2\nAL_FORMAT_STEREO16\tsample * 2 * 2\u003c/strong\u003e\n\u003c/pre\u003e"