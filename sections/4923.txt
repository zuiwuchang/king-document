var __v="\u003cpre class='k-pre'\u003e如果 有個 NewLogger 用來 替代 舊的 OldLogger\n配置 \u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e[ NewLogger,\n  // Not aliased! Creates two instances of `NewLogger`\n  { provide: OldLogger, useClass: NewLogger}]\u003c/pre\u003e\n\u003cpre class='k-pre'\u003e使用 NewLogger 和 OldLogger 的代碼 會分別創建一個 NewLogger OldLogger\n此時 可以 使用 useExisting 指定 OldLogger 是 NewLogger 的別名 從而可以不用修改 代碼 讓 使用 OldLogger 的代碼 也使用 NewLogger\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e[ NewLogger,\n  // Alias OldLogger w/ reference to NewLogger\n  { provide: OldLogger, useExisting: NewLogger}]\u003c/pre\u003e"