var __v="\u003cpre class=\"k-pre\"\u003e//傳入一個 前攝器 io_service 構造\n\n//連接到一個指定的 端點\nvoid connect(const endpoint_type\u0026amp; peer_endpoint)\n//異步連接 同上 不過多一個 完成處理函數 作為參數\nasync_connect\n\n//返回連接的 兩端 端點信息\nlocal_endpoint\nremote_endpoint\n\n//同步 讀寫 數據 需要一個讀寫緩衝區作為參數 \nread_some\nwrite_some\n\n//異步 讀寫 參數同上 不過多了一個 完成處理函數 \nasync_read_some\nasync_write_some\n\n//關閉socket 析構時被 自動調用\nclose\n\u003c/pre\u003e"