var __v="\u003cpre class=\"k-pre\"\u003etar 提供了 打包多個 檔案的 能力 同時 可以使用 gzip bzip2 等進行 壓縮\n//打包\ntar -jcvf dist src\n\n//查看包\ntar -jtf src\n\n//解壓\ntar -jxvf src -C dist\n\n\t-c\t打包\n\t-t\t查詢包中檔案名\n\t-x\t解包\n\n\t-J\t使用 xz  算法\n\t-j\t使用 bzip2 算法\n\t-z\t使用 gzip 算法\n\n\t-v\t顯示正在處理的檔案名\n\n\t-f\t指定打包 檔案名\n\t-C\t接包時 指定 解包目錄名\n\n\t-p\t保留 檔案 原本權限 屬性(可用於備份)\n\t-P\t保留 絕對路徑\n\t--exclude=FILE\t打包時 排除FILE 多個 FILE 以空格隔開\n\u003c/pre\u003e"