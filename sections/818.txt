var __v="\u003cpre class=\"k-pre\"\u003e//運行容器 同時將/webapp 掛接到容器\nsudo docker run -it --name kt -v /home/king:/webapp ubuntu /bin/bash\n\t\u003cstrong\u003e-v p1:p2 將主機的 p1 mount 到 容器的 p2\n\t(p1 如果在主機中不存在 將自動在主機中 創建\n\tp1: 參數可省略)\u003c/strong\u003e\n\n//只讀 掛接\nsudo docker run -it --name kt -v /home/king:/webapp\u003cstrong\u003e:ro\u003c/strong\u003e ubuntu /bin/bash\n\t\u003cstrong\u003e:ro\t指定 資料卷 爲只讀 (默認 讀寫)\u003c/strong\u003e\n\n\n//將 檔案進行 掛接\nsudo docker run -it -v ~/.bash_history:/.bash_history ubuntu /bin/bash\n\u003c/pre\u003e"