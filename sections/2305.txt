var __v="\u003cpre class=\"k-pre\"\u003e# 檔案下載 位置 \n# 務必使用絕對 路徑 (rtorrent 存在bug 有時會 忽略相對路徑)\ndirectory = /home/king/rt/dowm\n\n# 下載檔名編碼\nencoding_list = UTF-8\n\n# 用於 保存 torrents 進度\nsession = /home/king/rt/.session\n\n#全局 下載/上傳 速度 限制 (單位 kb) 爲 0 則不限制\ndownload_rate = 200\nupload_rate = 28\n\n\n#每個種子允許的 最大/最小 連接\n#min_peers = 40\nmax_peers = 52\n\n# 同上 不過是針對 已完成的 種子 (-1 表示和下載中的種子 一致)\n#min_peers_seed = 10\nmax_peers_seed = 1\n\n# 每個種子 最大 同上 上傳 數\nmax_uploads = 1\n\n# 下載完成/重新啓動時 是否 hash效驗\ncheck_hash = yes\n\n# 監聽目錄中的 新種子 自動進行 下載\n#schedule = watch_directory,5,5,load_start=./watch/*.torrent\n#schedule = untied_directory,5,5,stop_untied=\n#schedule = watch_directory,5,5,load_start=/home/[user]/watch/*.torrent\n#schedule = untied_directory,5,5,stop_untied=\n#schedule = tied_directory,5,5,start_tied=\n\n#磁盤空間不足時 停止 自動下載\n#schedule = low_diskspace,5,60,close_low_diskspace=100M\n\n#監聽 端口\nport_range=49164-49164\n\n\n# 加密 默認爲 0 其它可選值如下\n# allow_incoming, try_outgoing, require, require_RC4, enable_retry, prefer_plaintext\nencryption = allow_incoming,try_outgoing,enable_retry\n\ndht = auto\n# dht_port = 6881\n\npeer_exchange = yes\n\u003c/pre\u003e"