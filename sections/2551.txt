var __v="\u003cpre class=\"k-pre\"\u003e無法 模擬一個 delete 操作 lua自動 管理內存\n若想一個 函數參入 自身 即使是 self 也不過是對引用計數 +1 在函數中 為self=nil 不過是引用計數 -1\n故 無法 寫一個 函數 刪除 實例化的對象(lua 應該是 一個 變量[從基類獲取原型的 變量]) \n\u003c/pre\u003e"