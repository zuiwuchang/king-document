var __v="\u003cpre class=\"prettyprint linenums\"\u003e//遍歷元素 對被個元素都 調用 function \n//返回 false 將終止遍歷後續元素\n//index 為元素索引 從0開始計算 element為當前元素 (兩個都為可選參數)\n$(\"input\").each(function(index, element) {\n\tif(index \u0026gt; 0){\n\t\treturn false;\n\t\t}\n\talert(index);\n\t});\n\n//操縱 第一個被 選中 元素 \nalert($(\"input\").attr(\"name\"));\n\n//設置所有 p 的值\n$(\"input\").val(\"123\");\n\n\n//返回元素個數\nalert($(\"input\").size());\nalert($(\"input\").length);\u003c/pre\u003e"