var __v="\u003cpre class='prettyprint linenums'\u003e// 定義一個 函數 接口\ninterface IFunc {\n    (str: string): void\n}\nfunction test(f: IFunc): void {\n    f(\"123\")\n}\n\ntest(function (str) {\n    console.log(str)\n})\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e// 定義一個 接口\ninterface IAnimal {\n    // 定義 一個 rw 屬性\n    Name: string\n\n    // 定義一個 只讀 屬性\n    readonly Type: string\n\n    // 使用 ? 定義 可選 屬性\n    Lv?: number\n\n    // 定義 方法\n    Eat(): void\n    // 同樣 可以指定 可選\n    Speak?(str: string): void\n}\n\n// 傳入 接口\nfunction Show(obj: IAnimal): void {\n    console.log(\"this is a\", obj.Type)\n    if (obj.Lv) {\n        console.log(obj.Name, obj.Lv)\n    } else {\n        console.log(obj.Name)\n    }\n\n    obj.Eat()\n    if (obj.Speak) {\n        obj.Speak(\"ok\");\n    }\n}\n\nShow({\n    Type: \"Cat\",\n    Name: \"cc\",\n    Eat: function (): void {\n        console.log(\"i'm a\", this.Type)\n        console.log(this.Name, \"eat\");\n    },\n    Speak: function (str: string): void {\n        console.log(str, \"喵~\")\n    },\n})\nShow({\n    Type: \"Dog\",\n    Name: \"dd\",\n    Lv: 1,\n    Eat: function (): void {\n        console.log(\"i'm a\", this.Type)\n        console.log(this.Name, \"eat\");\n    },\n})\u003c/pre\u003e"