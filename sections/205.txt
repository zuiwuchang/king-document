var __v="\u003cpre class=\"prettyprint linenums\"\u003eimport \"fmt\"\n\nfunc sum(a []int, c chan int) {\n\tsum := 0\n\tfor _, v := range a {\n\t\tsum += v\n\t}\n\tc \u0026lt;- sum // 将和送入 c\n}\n\nfunc main() {\n\ta := []int{7, 2, 8, -9, 4, 0}\n\n\tc := make(chan int)\n\tgo sum(a[:len(a)/2], c)\n\tgo sum(a[len(a)/2:], c)\n\tx, y := \u0026lt;-c, \u0026lt;-c // 从 c 中获取\n\n\tfmt.Println(x, y, x+y)\n}\u003c/pre\u003e"