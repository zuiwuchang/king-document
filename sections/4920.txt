var __v="\u003cpre class='k-pre'\u003e服務提供商 告訴了 注入系統 此服務 應該被哪個 注入器 來負責創建\n\n默認生成的 代碼\nprovidedIn: 'root' 指定了 由 根本 模塊 創建 故 在應用的 任何 地方都可注入\n\nprovidedIn 還可以支持 一個 @NgModule 模塊 這樣的 化 此模塊 和私有 imports ng模塊的 地方都可用\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e@Injectable({\n  providedIn: BeautyModule,\n})\u003c/pre\u003e\n\u003cpre class='k-pre'\u003e除了 在 @Injectable 裝飾器上 指定外 \n@NgModule 和 @Component 都支持 providers 屬性\nproviders 是一個 服務提供商 數組 然可以再此 注入 服務提供商 到注入器\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003eimport { Component, OnInit } from '@angular/core';\nimport { Beauty, BeautyService } from '../data/beauty.service'\n@Component({\n  selector: 'app-home',\n  templateUrl: './home.component.html',\n  styleUrls: ['./home.component.css'],\n  providers: [BeautyService],\n})\nexport class HomeComponent implements OnInit {\n  beautys: Beauty[];\n  val: string;\n  constructor(private beautyService: BeautyService) {\n  }\n\n  ngOnInit() {\n    this.beautys = this.beautyService.get();\n  }\n\n  onAdd() {\n    this.beautyService.put(this.val);\n  }\n}\n\u003c/pre\u003e"