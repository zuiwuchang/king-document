var __v="\u003cpre class=\"k-pre\"\u003etuple 是一個 固定的(不可修改) 有序序列\n其中包含0個或多個對象的引用\n\ntuple支持 字符串一樣的 分片 佈距語法\n\n如果需要修改 tuple 可以使用 list() 將其轉換爲 list 後需要產生的 list\n\n\n//切片\n[:]\n\n//將 tuple 重複 指定次數 (如果重複0次 則tuple 變爲空 tuple () )\n*=\n\t(1,2) *= 2\t#(1,2,1,2)\n//將 兩個 tuple 合併\n+=\n\t(1,2) += (3,)\t#(1,2,3)\n\n//返回 v 在 tuple 中出現的 次數\ntuple.count(v)\n\n//返回 v 在 tuple 中出現的 位置 不存在 拋出 ValueError 異常\ntuple.index(v)\n\u003c/pre\u003e"