var __v="\u003cpre class=\"k-pre\"\u003euseradd 新建帳號的 密碼 是無效值(/etc/shadow)\n故無法登入 此時可以使用 passwd 指令 修改密碼\n\n//修改當前用戶密碼 (需要輸入當前密碼)\npasswd\n\n//直接修改 指定帳號的 密碼 (需要root權限)\npasswd 帳號名\n\t-l\t//修改 /etc/shadow 在密碼前加上 ! 以使密碼無效\n\t-u\t//... 去掉密碼前的 ! (如果存在 !的話)\n\t-n number\t//修改密碼 不可更改天數 (相對當前日期)\n\t-x number\t//...過期 (相對當前日期)\n\t-w number\t//...警告\n\t-i number\t//...失效\n\n//禁止 dark 密碼\nsudo passwd dark\n\n//禁止 dark 登入\nsudo passwd -l dark\n\n//允許 dark 登入\nsudo passwd -u dark\n\n\n\u003cstrong\u003epasswd 不允許設置若口令\npasswd 帳號名 則沒有此限制\u003c/strong\u003e\n\u003c/pre\u003e"