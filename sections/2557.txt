var __v="\u003cpre class=\"k-pre\"\u003estring是字符串庫\nlua5.1開始可以 簡寫對string庫的 調用\nstring.upper(s) 同 s:upper()\n\n\n//返回字符串 長度\nlen(str)\n//將n個str連接為一個字符串 返回\nrep(str,n)\n\n//返回 全 大寫/小寫 結構\nupper(str)\nlower(str)\n\n//返回截取的字符串 [i,j] (索取是從1開始)\n(i j可取負值 -1[最後一個字符] -2[倒數第二個] ...)\nsub(str,i,j) \n\n//返回 編碼byte 對應字符\nchar(b)\n//返回字符串中 第 i個字符對應編碼\n//（同 sub i可取 負值）\nbyte(str,i=1)\n//返回 [i,j] 所有字符編碼 byte*\nbyte(str,i,j)\n\n//格式化字符串 類似c printf\nstring.format\n\u003c/pre\u003e\n"