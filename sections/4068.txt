var __v="\u003cpre class=\"k-pre\"\u003e每個 clip 就是 一組動畫 執行的 數據\n在 資源編輯器中 直接 新建後 便可在 動畫編輯器中 編輯動畫\n\n如果一個 Animation  存在多個 clip\n動畫編輯器會 存在 bug 只能編輯第一個 clip\n所以建議 創建一個單獨 的 節點 專門用於 編輯 clip\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003ecc.Class({\n    extends: cc.Component,\n\n    properties: {\n        //是否 啓用 節點\n        Enable:true,\n    },\n\n    // use this for initialization\n    onLoad: function () {\n        if(this.Enable){\n            return;\n        }\n\n        //刪除本節點\n        this.node.removeFromParent();\n    },\n});\n\u003c/pre\u003e"