var __v="\u003cpre class=\"k-pre\"\u003e此方法 對對話框無效\n在創建窗口前 MFC 會調用 PreCreateWindow \n修改PreCreateWindow傳入的 CREATESTRUCT\u0026amp; cs 既可修改窗口外觀\n\ntypedef struct tagCREATESTRUCT {\n    LPVOID lpCreateParams;\n    HINSTANCE hInstance;\n    HMENU hMenu;\t\t\t\t\t//菜單\n    HWND hwndParent;\n    int cy;\n    int cx;\n    int y;\n    int x;\n    LONG style;\t\t\t\t\t\t//窗口風格\n    LPCTSTR lpszName;\t\t\t\t//窗口標題\n    LPCTSTR lpszClass;\t\t\t\t//窗口類名\n    DWORD dwExStyle;\t\t\t\t//擴展風格\n} CREATESTRUCT, *LPCREATESTRUCT;\n\n\n\nBOOL CMainFrame::PreCreateWindow(CREATESTRUCT\u0026amp; cs)\n{\n\tif( !CFrameWnd::PreCreateWindow(cs) )\n\t\treturn FALSE;\n\t// TODO: 在此处通过修改\n\t//  CREATESTRUCT cs 来修改窗口类或样式\n\tcs.x=0;\n\tcs.y=0;\n\tcs.cx=400;\n\tcs.cy=400;\n\tcs.lpszName=L\"123\";\n\tcs.style \u0026amp;= ~FWS_ADDTOTITLE;\n\n\treturn TRUE;\n}\n\n//修改窗口類屬性\nDWORD SetClassLong(          HWND hWnd,\n    int nIndex,\t\t\t//要獲取什麼屬性\n    LONG dwNewLong\n);\nnIndex取值\nGCL_WNDPROC\nGCL_STYLE\nGCL_HBRBACKGROUND\t//背景畫刷\nGCL_HCURSOR\t\t\t//光標\nGCL_HICON\t\t\t//圖標\n...\n\t\t\n//獲取窗口類屬性\nDWORD GetClassLong(          HWND hWnd,\n    int nIndex\n);\n\n//根據資源ID 返回 資源對應名\nLPTSTR MAKEINTRESOURCE(\n    WORD wInteger\n);\n\u003c/pre\u003e"