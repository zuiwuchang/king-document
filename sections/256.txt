var __v="\u003cpre class=\"k-pre\"\u003e上面example2示例使用 += 連接字符 這通常是低效的 \n通常的做法是將字符保存到切片中 在一次轉化為字符串 如\n\u003cpre class=\"prettyprint linenums\"\u003epackage main\n\nimport (\n\t\"fmt\"\n\t\"unicode/utf8\"\n)\n\nfunc main() {\n\tstr := \"1234567890\"\n\tnew_str := make([]rune, utf8.RuneCountInString(str))\n\n\tfor i, char := range []rune(str) {\n\n\t\tnew_str[i] = char\n\t}\n\tfmt.Println(string(new_str))\n}\u003c/pre\u003e\n\u003cstrong\u003eGo 有更方便的方式 bytes.Buffer 完成了類似上面的功能\u003c/strong\u003e\u003c/pre\u003e"