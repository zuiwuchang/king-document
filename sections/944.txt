var __v="\u003cpre class=\"k-pre\"\u003estr=\"this is SHA-512\";\nSystem.out.println(str );\n\u003cstrong\u003eMessageDigest sh=MessageDigest.getInstance(\"SHA-512\");\t//括号传入要使用的 算法名称 不区分大小写\u003c/strong\u003e\nsh.update(str.getBytes());\ndate=sh.digest();\n\u003cstrong\u003e//date=sh.digest(str.getBytes());\t//上两句可用 此句替换\u003c/strong\u003e\ndate=Hex.encode(date);\nstr=new String(date);\nSystem.out.println(str);\nSystem.out.println();\n\u003c/pre\u003e"