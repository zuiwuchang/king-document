var __v="\u003cpre class='k-pre'\u003e使用 ElementRef 可以將 宿主 的 DOM 引用 注入 以便修改 DOM\n使用 Renderer2 可以操作 DOM 的 css 樣式 和 DOM attribute\n使用 Input 可以 定義 輸入 屬性\n\u003c/pre\u003e\n\n\u003cpre class='prettyprint linenums'\u003eimport { Directive, ElementRef, Renderer2, Input } from '@angular/core';\n\n@Directive({\n  selector: '[appShow]'\n})\nexport class ShowDirective {\n  //注入 宿主\n  constructor(private el: ElementRef, private renderer: Renderer2) {\n  }\n\n  // 定義一個 和 屬性 同名的 輸入 屬性\n  @Input()\n  set appShow(ok: boolean) { //必須使用 set 才能 監聽 值改變\n    if (ok) {\n      this.renderer.removeClass(this.el.nativeElement, 'Hide');\n    } else {\n      this.renderer.addClass(this.el.nativeElement, 'Hide');\n    }\n  }\n}\n\u003c/pre\u003e\n\n\u003cpre class='prettyprint linenums'\u003e\u0026lt;button (click)=\"show=!show\"\u0026gt;do\u0026lt;/button\u0026gt;\n\u0026lt;div [appShow]=\"show\"\u0026gt;\n  home view\n\u0026lt;/div\u0026gt;\u003c/pre\u003e"