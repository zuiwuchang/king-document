var __v="\n\u003cpre class='prettyprint linenums'\u003e#include \u0026lt;iostream\u0026gt;\n#include \u0026lt;string\u0026gt;\n#include \u0026lt;vector\u0026gt;\n#include \u0026lt;cstdint\u0026gt;\n#include \u0026lt;zlib/zlib.h\u0026gt;\n#include \u0026lt;cassert\u0026gt;\n\n\n\nint main()\n{\n    //原文\n    std::string str = \"草泥馬 funcking 的 test\";\n    //壓縮後\n    std::vector\u0026lt;std::uint8_t\u0026gt; compress(200,0);\n    //解壓後\n    std::vector\u0026lt;std::uint8_t\u0026gt; uncompress(200,0);\n\n    std::size_t size = compress.size();\n    assert(Z_OK == ::compress(compress.data(),\u0026amp;size,(const Bytef*)str.data(),str.size()));\n\n    std::size_t un_size = uncompress.size();\n    assert(Z_OK == ::uncompress(uncompress.data(),\u0026amp;un_size,(const Bytef*)compress.data(),size));\n\n    assert(std::string((char*)uncompress.data(),un_size) == str);\n\n    return 0;\n}\n\u003c/pre\u003e"