var __v="\u003cpre class=\"k-pre\"\u003eGUI線程負責創建窗口 以及處理主消息循環 \n一般而言GUI線程絕對不會去做那些不能夠馬上完成的工作\nworker線程負責執行純粹的運算工作\n\ngui線程 擁有消息隊列的線程 任何一個特定窗口的消息總是被產生這一窗口的線程抓取並處理\n所有對此窗口的改變也都應該由該線程完成\n\n如果worker線程創建了一個窗口 那麼就會有消息隊列被付着與此線程 於是worker線程變成了GUI線程\n如果worker線程需要輸入輸出 應該授權給UI線程來做 並且將結果通知給worker線程\n\u003c/pre\u003e"