var __v="\u003cpre class=\"k-pre\"\u003e//異步 讀取檔案 完整內容\nfs.readFile(file[, options], callback)\n\n//同readFile 不過 同步讀取\n//讀取內容 作爲 返回值返回\n//出現錯誤 throw\nfs.readFileSync(file[, options])\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e\"use strict\"\n\nglobal.alert = console.log\n\nvar fs = require(\"fs\")\n\n//讀取 到字符串\nfs.readFile(\"main.js\", \"utf8\", function(err,data){\n\tif(err){\n\t\talert(err);\n\t\treturn\n\t}\n\n\talert(\"data :\")\n\talert(data)\n});\n\n//讀取 Buffer\nfs.readFile(\"main.js\",  function(err,data){\n\tif(err){\n\t\talert(err);\n\t\treturn\n\t}\n\n\talert(\"data :\")\n\talert(data.toString(\"utf8\"));\n});\u003c/pre\u003e"