var __v="\u003cpre class=\"k-pre\"\u003egit 協議的 配置 要比 http https 複雜\n需要 依賴另外的 組件 socat\u003c/pre\u003e\n\u003col\u003e\n\t\u003cli\u003e\n\t\t安裝 socat\n\u003cpre class=\"prettyprint linenums\"\u003esudo apt install socat\u003c/pre\u003e\n\t\u003c/li\u003e\n\t\u003cli\u003e\n\t\t編輯一個 腳本 並且加上 x 權限\n\u003cpre class=\"prettyprint linenums\"\u003evi ~/bin/gitproxy.sh\nchmod u+x ~/bin/gitproxy.sh\u003c/pre\u003e\n\u003cstrong\u003ehttp https 代理\u003c/strong\u003e \n\u003cpre class=\"prettyprint linenums\"\u003e#!/bin/sh\n# Use socat to proxy git through an HTTP CONNECT firewall.\n# Useful if you are trying to clone git:// from inside a company.\n# Requires that the proxy allows CONNECT to port 9418.\n#\n# Save this file as gitproxy somewhere in your path (e.g., ~/bin) and then run\n#   chmod +x gitproxy\n#   git config --global core.gitproxy gitproxy\n#\n# More details at http://tinyurl.com/8xvpny\n\n# Configuration. Common proxy ports are 3128, 8123, 8000.\n_proxy=localhost\n_proxyport=8118\n\nexec socat STDIO PROXY:$_proxy:$1:$2,proxyport=$_proxyport\n\u003c/pre\u003e\n\u003cstrong\u003eSOCKS 代理\u003c/strong\u003e \n\u003cpre class=\"prettyprint linenums\"\u003e#!/bin/sh\n#\n# Use socat to proxy git through a SOCKS proxy.\n# Useful if you are trying to clone git:// from inside a company.\n#\n# See http://tinyurl.com/8xvpny for Emil Sit's original HTTP proxy script.\n# See http://tinyurl.com/45atuth for updated SOCKS version.\n#\n\n# Configuration.\n_proxy=localhost\n_proxyport=1080\n\nexec socat STDIO SOCKS4:$_proxy:$1:$2,socksport=$_proxyport\u003c/pre\u003e\n\t\u003c/li\u003e\n\t\u003cli\u003e\n\t\t配置 git\n\u003cpre class=\"prettyprint linenums\"\u003egit config --global  core.gitProxy  '~/bin/gitproxy.sh'\u003c/pre\u003e\n\t\u003c/li\u003e\n\u003c/ol\u003e"