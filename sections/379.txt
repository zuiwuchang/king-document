var __v="\u003cpre class=\"k-pre\"\u003eLinux 下非root是不能啟用[1,1024] 端口的\n故要以非root運行tomcat 而使用80端口只能使用 iptable 將1024以上端口 轉發到80\n且在iptable中 要同時允許訪問 80 和 tomcat使用的端口\n\n\u003cpre class='prettyprint linenums'\u003eiptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 8080\nsudo iptables -I INPUT 1 -p tcp -m tcp --dport 8080 -j ACCEPT\nsudo iptables -I INPUT 1 -p tcp -m tcp --dport 80 -j ACCEPT\n\u003c/pre\u003e\u003c/pre\u003e\n"