var __v="\u003cpre class=\"k-pre\"\u003eImageCodecInfo 存儲了 編碼器/解碼器 詳細信息\nclass ImageCodecInfo {  public:              \n\t\u003cstrong\u003eCLSID        Clsid;\t\t\t\t//編碼標識\u003c/strong\u003e\n\tGUID         FormatID;\t\t\t\t//文件格式標識\n\t\u003cstrong\u003econst WCHAR* CodecName;\t\t\t\t//編碼器名\u003c/strong\u003e\n\tconst WCHAR* DllName;\t\t\t\t//編碼器依存的動態庫\n\tconst WCHAR* FormatDescription;\t\t\t//編碼描述\n\t\u003cstrong\u003econst WCHAR* FilenameExtension;\t\t\t//編碼器對於文件擴展名\n\tconst WCHAR* MimeType;\t\t\t\t//編碼器的MIME(用於網際郵件擴充協議)類型描述\u003c/strong\u003e\n\tDWORD        Flags;\t\t\t\t//ImageCodeFlags枚舉的標記集\n\tDWORD        Version;\t\t\t\t//編碼器版本\n\tDWORD        SigCount;\t\t\t\t//餘編碼器對應文件格式標記總數\n\tDWORD        SigSize;\t\t\t\t//文件格式標記大小\n\tconst BYTE*  SigPattern;\t\t\t//標記的樣式\n\tconst BYTE*  SigMask;\t\t\t\t//標記掩碼\n\t};\n    \n//將 CLSID 轉換為字符串    \nint StringFromGUID2(\n\tREFGUID rguid,\t\t//clsid\n\tLPOLESTR lpsz,\t\t//字符串數組指針\n\tint cchMax\t\t\t//字符串數組大小 sizeof(array)\n);\n\u003c/pre\u003e"