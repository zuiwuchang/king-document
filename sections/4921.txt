var __v="\u003cpre class='k-pre'\u003e對於 相同 注入器 對於同一個 依賴項 都只會返回 同個 單例\n\n雖然可以在 多個地方 註冊 服務提供商 但\n首推 @Injectable (Angular 可以識別到 沒被使用的 @Injectable 註冊 從而在發佈版本 將其刪除 )\n其次 是在 @Component 中 (如果需要 組件 擁有 不同的 依賴實例 則在此 註冊)\n一般 不會在 @NgModule 中 註冊 (除非 你 這個模塊是 墮性加載的 你想 得到模塊加載時 才創建 依賴實例)\u003c/pre\u003e"