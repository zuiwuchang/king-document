var __v="\u003cpre class=\"k-pre\"\u003ec# 使用 參數修飾符 限定 參數如何 傳遞給函數\n\n無\t\t值傳遞\nout\t\t輸出參數 引用傳遞 且函數必須為其設置值\nref\t\t同 out 只是 函數可以 不必一定要為 參數 設置值\nparams\t只能在最後一個 參數上限定(且 參數型別必須為數組) 代表 不定長參數 類似golang的 ...\n\u003c/pre\u003e"