var __v="\u003cpre class=\"k-pre\"\u003eunsafe.Pointer 可以獲取 go結構到 內存地址\n(unsafe.Pointer 會對 引用計數造成影響)\n\nstring slice 實際上是 StringHeader SliceHeader\n通過 unsafe.Pointer 可以 載兩者間 轉化的同時 繞開 go到 copy 操縱\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003etype StringHeader struct {\n    Data uintptr\n    Len  int\n}\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003etype SliceHeader struct {\n    Data uintptr\n    Len  int\n    Cap  int\n}\u003c/pre\u003e"