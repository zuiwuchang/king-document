var __v="\u003cpre class='k-pre'\u003e實現 ngOnChanges 接口 可以 監控 所有 輸入屬性的 變化 不過 無法 修改值\n(只有 外部 修改 才會 回調 在內部 修改 不會 回調)\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003eimport { Component, OnInit, Input, OnChanges, SimpleChange } from '@angular/core';\n\n@Component({\n  selector: 'app-beauty',\n  templateUrl: './beauty.component.html',\n  styleUrls: ['./beauty.component.css']\n})\nexport class BeautyComponent implements OnInit, OnChanges {\n  @Input() name: string = 'ok';\n  @Input() val: string;\n\n  constructor() { }\n\n  ngOnInit() {\n  }\n  // 監視 所有 輸入屬性 變化\n  ngOnChanges(changes: { [propKey: string]: SimpleChange }) {\n    for (let key in changes) {\n      const obj = changes[key];\n      console.log(key, \"change to\", obj.currentValue)\n\n      // 修改值\n      if (obj.firstChange) {\n        console.log(\"first change\");\n      }\n    }\n  }\n}\n\u003c/pre\u003e"