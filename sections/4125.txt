var __v="\u003cpre class=\"k-pre\"\u003e代碼演示了如何使 指定顏色 全部變成透明\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e\n//創建一個 mask\ntype Alpha struct {\n\tsrc   image.Image\n\tpoint image.Point\n}\n\nfunc (a *Alpha) ColorModel() color.Model {\n\treturn color.AlphaModel\n}\n\nfunc (a *Alpha) Bounds() image.Rectangle {\n\treturn a.src.Bounds()\n}\nfunc (a *Alpha) At(x, y int) color.Color {\n\tsrc := a.src\n\tmin := a.point\n\tif src.At(x, y) != src.At(min.X, min.Y) {\n\t\treturn color.Alpha{255}\n\t}\n\treturn color.Alpha{0}\n}\nfunc newImage(src image.Image) (image.Image, error) {\n\t//創建新圖像\n\tbounds := src.Bounds()\n\tdist := image.NewRGBA(src.Bounds())\n\n\t//合成圖像\n\tdraw.DrawMask(dist,\n\t\tbounds,\n\t\tsrc,\n\t\tbounds.Min,\n\t\t\u0026amp;Alpha{src, bounds.Min},\n\t\tbounds.Min,\n\t\tdraw.Over,\n\t)\n\n\t//返回 Image 接口\n\treturn dist.SubImage(bounds), nil\n}\u003c/pre\u003e"