var __v="\u003cpre class='k-pre'\u003euseFactory 可以指定從一個 工廠函數 創建 服務\n工廠函數 也可以 依賴 其它 服務 作爲函數 傳入即可 同時需要 使用 deps 屬性 指定 依賴的 服務 其供應商\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003eproviders: [\n    {\n      provide: BeautyProxyService,\n      useFactory: (beauty: BeautyService) =\u0026gt; {\n        return {\n          get() {\n            return beauty.get()\n          },\n          put(name) {\n            beauty.put(\"proxy-\" + name)\n          }\n        }\n      },\n      deps: [BeautyService],\n    }\n  ],\u003c/pre\u003e"