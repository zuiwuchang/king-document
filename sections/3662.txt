var __v="\u003cpre class=\"k-pre\"\u003e默認 ulimit 不會被設置 此時 任何 用戶 都可以 使用 ulimit 設置自己需要的 資源\n\n如果 需要 限制 特定用戶 的 ulimit 可以 修改\n/etc/security/limits.conf\n在其中 爲其 指定 ulimit\n\n//爲 king 設置 ulimit -Hn 1024\n#domain\ttype\titem\tvalue\nking    hard    nofile  1024\n\t\u003cstrong\u003edomain 指定了 受限人 \n\t可以是一個 用戶名(king)\n\t可以是一個 @組名(@king)\n\t* 所有用戶\n\n\ttype 指定了 如何限制 可以是如下三個值\n\tsoft\n\thard\n\t-\n\n\titem 指定了 限制 條目\n\t#        - core - limits the core file size (KB)\n\t#        - data - max data size (KB)\n\t#        - fsize - maximum filesize (KB)\n\t#        - memlock - max locked-in-memory address space (KB)\n\t#        - nofile - max number of open file descriptors\n\t#        - rss - max resident set size (KB)\n\t#        - stack - max stack size (KB)\n\t#        - cpu - max CPU time (MIN)\n\t#        - nproc - max number of processes\n\t#        - as - address space limit (KB)\n\t#        - maxlogins - max number of logins for this user\n\t#        - maxsyslogins - max number of logins on the system\n\t#        - priority - the priority to run user process with\n\t#        - locks - max number of file locks the user can hold\n\t#        - sigpending - max number of pending signals\n\t#        - msgqueue - max memory used by POSIX message queues (bytes)\n\t#        - nice - max nice priority allowed to raise to values: [-20, 19]\n\t#        - rtprio - max realtime priority\n\n\tvalue 指定了 限制值\u003c/strong\u003e\n\u003c/pre\u003e"