var __v="\u003cpre class=\"k-pre\"\u003eif(expr)\n\tcommand1(arg)\nelse()\n\tcommand2(arg)\nendif()\n\n較早版本 使用如下 寫法\nif(expr)\n\tcommand1(arg)\nelse(expr)\n\tcommand2(arg)\nendif(expr)\n\n\n//使用 AND OR\nif((expr) AND (expr OR (expr)))\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e# 下面兩行意義相同\nif (foo)\nif (${foo})\n\n# 下面兩行意義相同\nif (foo AND bar)\nif (${foo} AND ${bar})\u003c/pre\u003e"