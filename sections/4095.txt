var __v="\u003cpre class=\"k-pre\"\u003e射線檢測 用來 檢測 給定的線段穿過哪些碰撞體 可以獲取到碰撞體的法線向量等 信息\nvar results = cc.director.getPhysicsManager().rayCast(p1, p2, type);\n\nfor (var i = 0; i \u003c results.length; i++) {\n    var result = results[i];\n    var collider = result.collider;\n    var point = result.point;\n    var normal = result.normal;\n    var fraction = result.fraction;\n}\n\ntype 可以是如下值\n\tcc.RayCastType.Any\t檢測射線路徑上帝 任意碰撞體 一旦檢測到碰撞體 立刻返回 最快\n\tcc.RayCastType.Closest\t\u003cstrong\u003e(默認)\u003c/strong\u003e 檢測路徑上最近的碰撞體 稍慢\n\tcc.RayCastType.All\n\t檢測路徑上所有碰撞體 (檢測結果順序不固定) 且同個碰撞體可能返回多次 慢\n\tcc.RayCastType.AllClosest\n\t檢測路徑上所有碰撞體 但會對返回值篩選 只返回碰撞體距離 射線起始點 最近哪個點的 信息 最慢\n\n返回值\n\tcollider\t射線穿過的碰撞體\n\tpoint\t射線和碰撞體的相交點\n\tnormal\t碰撞體在相交的表面的法線向量\n\tfraction\t相交點在射線上的分數\n\u003cimg src='/sections/4095/image/raycasting-output.png' style=\"max-width: 100%;\"\u003e\n\u003c/pre\u003e\n"