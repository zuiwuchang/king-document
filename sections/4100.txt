var __v="\u003cpre class=\"k-pre\"\u003e//獲取移動速度\nvar velocity = rigidbody.linearVelocity;\n//設置移動速度\nrigidbody.linearVelocity = velocity;\n\n移動速度衰減系數 可以用來模擬空氣摩擦力等效果 它會使現有速度 越來越慢\n//獲取 移動速度衰減系數\nvar damping = rigidbody.linearDamping;\n//設置 移動速度衰減系數\nrigidbody.linearDamping = damping;\n\n有時需要獲取剛體上某個點的移動速度 比如一個盒子旋轉向前飛 碰到了牆 可能會期望獲取盒子在發生碰撞的點的速度 可以使用 getLinearVelocityFromWorldPoint 獲取\nvar velocity = rigidbody.getLinearVelocityFromWorldPoint(worldPoint);\n亦可傳入一個 cc.Vec2 作爲第二個參數的 返回值 獲取(以避免創建過多對象 來提高效率)\nvar velocity = cc.v2();\nrigidbody.getLinearVelocityFromWorldPoint(worldPoint, velocity);\n\t\u003cstrong\u003e(剛體的get方法都提供了out參數來接收函數返回值)\u003c/strong\u003e\n\u003c/pre\u003e"