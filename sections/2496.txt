var __v="\u003cpre class=\"k-pre\"\u003elua 要求 迭代器為一個函數 每次調用 都返回集合的 下一個 元素\n返回 nil 代表介紹遍歷\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e//定義一個 數組 迭代器\nfunction values(t)\n\tlocal i\t=0\n\treturn function ()\n\t\t\t\ti\t=\ti\t+\t1\n\t\t\t\treturn t[i]\n\t\t\tend\nend\n\n//遍歷之\nt\t=\t{1,2,3}\niter\t=\tvalues(t)\nwhile true do\n\tlocal v = iter()\n\tif v == nil then\n\t\tbreak\n\tend\n\n\tprint(v)\n\nend\n\n\n\n//泛型for 只是簡化了上面的 while\nfor v in values(t) do\n\tprint(v);\nend\u003c/pre\u003e"