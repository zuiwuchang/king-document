var __v="\u003cpre class=\"k-pre\"\u003ezlib 提供了 compress compress2 uncompress 函數\n簡化 zlib 算法的 壓縮/解壓\n\n//压缩数据 成功返回 Z_OK\nint compress(\n\tByte *dest,\t\t\t//输出缓冲区\n\tuLong *destLen,\t\t//输出缓冲区大小 同时返回实际使用了多少字节输出缓冲区\n\tconst Byte *source,\t//输入缓冲区\n\tuLong sourceLen\t\t\t//输入缓冲区大小\n);\nint  compress2(\n\tBytef *dest,   \n\tuLongf *destLen,\n\tconst Bytef *source, \n\tuLong sourceLen,\n\tint level\t//壓縮等級\n);\n\n//解压数据 成功返回 Z_OK 数据不完整返回Z_DATA_ERROR\nint uncompress(\n\tByte *dest,\n\tuLong *destLen,\n\tconst Byte *source, \n\tuLong sourceLen\n);\n\u003c/pre\u003e"