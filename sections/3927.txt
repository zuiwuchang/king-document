var __v="\u003cpre class=\"k-pre\"\u003e除了 使用 SetConsoleCtrlHandler\nwin32 亦提供了 蹩腳的 signal 實現\n\n//發送一個 信號 到 正在執行的 程式\n//成功返回 0\nint raise(int sig );\n\n//爲 _SigNum 信號 註冊一個 處理 回調函數 \nvoid (__cdecl * __cdecl signal(_In_ int _SigNum, _In_opt_ void (__cdecl * _Func)(int)))(int);\n\n\u003cstrong\u003e#define SIGINT          2       /* interrupt */\u003c/strong\u003e\n#define SIGILL          4       /* illegal instruction - invalid function image */\n#define SIGFPE          8       /* floating point exception */\n#define SIGSEGV         11      /* segment violation */\n\u003cstrong\u003e#define SIGTERM         15      /* Software termination signal from kill */\u003c/strong\u003e\n#define SIGBREAK        21      /* Ctrl-Break sequence */\n#define SIGABRT         22      /* abnormal termination triggered by abort call */\n\u003c/pre\u003e"