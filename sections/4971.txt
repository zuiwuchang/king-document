var __v="\u003cpre class='k-pre'\u003eInput 屬性名 也可以 和 指令名 不同 不過 這樣 就不能 在 指定指令的 同時 指定屬性 需要 分開書寫\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003eimport { Directive, ElementRef, Renderer2, Input } from '@angular/core';\n\n@Directive({\n  selector: '[appShow]'\n})\nexport class ShowDirective {\n  //注入 宿主\n  constructor(private el: ElementRef, private renderer: Renderer2) {\n  }\n\n  // 定義一個 和 屬性 同名的 輸入 屬性\n  @Input()\n  set show(ok: boolean) { //必須使用 set 才能 監聽 值改變\n    if (ok) {\n      this.renderer.removeClass(this.el.nativeElement, 'Hide');\n    } else {\n      this.renderer.addClass(this.el.nativeElement, 'Hide');\n    }\n  }\n}\n\u003c/pre\u003e\n\u003cpre class='prettyprint linenums'\u003e\u0026lt;button (click)=\"show=!show\"\u0026gt;do\u0026lt;/button\u0026gt;\n\u0026lt;div appShow [show]=\"show\"\u0026gt;\n  home view\n\u0026lt;/div\u0026gt;\u003c/pre\u003e"