var __v="\u003cpre class=\"k-pre\"\u003eUrlBase64用其他符号替代了url中不允许存在的符号 以用于在url中传递二进制数据 但可用于替代的符号实在有限故不同版本实现的替代符可能不同\norg.bouncycastle.util.encoders.UrlBase64;\n\nSystem.out.println(\"this is UrlBase64\");\nde=\"UrlBase64\";\nen=\"解密后\";\nSystem.out.println(\"原文:\"+de);\n\u003cstrong\u003edate=UrlBase64.encode(de.getBytes());\u003c/strong\u003e\nen=new String(date);\nSystem.out.println(\"密文:\"+en);\n\u003cstrong\u003edate=UrlBase64.decode(en.getBytes());\u003c/strong\u003e\nde=new String(date);\nSystem.out.println(\"解密:\"+de);\nSystem.out.println();\n\u003c/pre\u003e"