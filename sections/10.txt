var __v="\u003cpre class=\"k-pre\"\u003e\n在 app.conf 中 可以 設置 自定義的 配置 只要滿足 key = val 的格式即可\n\n使用 revel.Config 是一個全局的 config.Config 保存了 app.conf  中的配置\n\u003c/pre\u003e\n\u003cpre class=\"prettyprint linenums\"\u003efunc (c *Context) Bool(option string) (result, found bool)\nfunc (c *Context) BoolDefault(option string, dfault bool) bool\nfunc (c *Context) Int(option string) (result int, found bool)\nfunc (c *Context) IntDefault(option string, dfault int) int\nfunc (c *Context) String(option string) (result string, found bool)\nfunc (c *Context) StringDefault(option, dfault string) string\u003c/pre\u003e"